import React from 'react';
// import PropTypes from 'prop-types';

const GridLayout = ({ children }) => {
	return (
		<>{ children }</>
	);
};

// NAME.defaultProps = {};

// NAME.propTypes = {};

export { GridLayout };
